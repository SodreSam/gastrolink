export interface CategoryProps {
    isSelected: boolean; // Prop para indicar se a categoria está selecionada
    hasSelectedCategory: boolean; // Prop para indicar se alguma categoria está selecionada
}
